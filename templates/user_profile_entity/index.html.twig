{% extends 'base.html.twig' %}

{% block title %}UserProfileEntity index{% endblock %}

{% block body %}
    <h1>UserProfileEntity index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>FirstName</th>
                <th>LastName</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for user_profile_entity in user_profile_entities %}
            <tr>
                <td>{{ user_profile_entity.id }}</td>
                <td>{{ user_profile_entity.firstName }}</td>
                <td>{{ user_profile_entity.lastName }}</td>
                <td>
                    <a href="{{ path('app_user_profile_entity_show', {'id': user_profile_entity.id}) }}">show</a>
                    <a href="{{ path('app_user_profile_entity_edit', {'id': user_profile_entity.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_user_profile_entity_new') }}">Create new</a>
{% endblock %}
