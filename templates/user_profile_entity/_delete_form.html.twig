<form method="post" action="{{ path('app_user_profile_entity_delete', {'id': user_profile_entity.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user_profile_entity.id) }}">
    <button class="btn">Delete</button>
</form>
