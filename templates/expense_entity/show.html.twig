{% extends 'base.html.twig' %}

{% block title %}ExpenseEntity{% endblock %}

{% block body %}
    <h1>ExpenseEntity</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ expense_entity.id }}</td>
            </tr>
            <tr>
                <th>Amount</th>
                <td>{{ expense_entity.amount }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ expense_entity.name }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ expense_entity.type }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ expense_entity.date ? expense_entity.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_expense_entity_index') }}">back to list</a>

    <a href="{{ path('app_expense_entity_edit', {'id': expense_entity.id}) }}">edit</a>

    {{ include('expense_entity/_delete_form.html.twig') }}
{% endblock %}
